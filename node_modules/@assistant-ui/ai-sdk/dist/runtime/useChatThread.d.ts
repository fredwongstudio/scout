import { AISDKRuntimeAdapter, CustomToCreateMessageFunction } from "./useAISDKRuntime.js";
import { InitializableThreadListItem } from "../transport/AssistantChatTransport.js";
import { ChatInit, ChatTransport } from "ai";
import { AssistantRuntime, ExternalStoreSharedOptions } from "@assistant-ui/core";
import { Chat, UIMessage as UIMessage$1 } from "@ai-sdk/react";
//#region src/runtime/useChatThread.d.ts
type ChatThreadOptions<UI_MESSAGE extends UIMessage$1 = UIMessage$1> = ChatInit<UI_MESSAGE> & ExternalStoreSharedOptions & {
  adapters?: AISDKRuntimeAdapter["adapters"] | undefined;
  toCreateMessage?: CustomToCreateMessageFunction;
  onResume?: AISDKRuntimeAdapter["onResume"];
  onResumeToolCall?: AISDKRuntimeAdapter["onResumeToolCall"];
  /**
   * Called when an automatic resumable stream reconnect fails. Use this to
   * surface a toast, report telemetry, or mark the thread as needing a
   * retry. The failed stream id is cleared after the callback unless a
   * newer id has replaced it.
   */
  onResumeError?: ((error: unknown) => void) | undefined;
  joinStrategy?: AISDKRuntimeAdapter["joinStrategy"];
};
type ChatThreadEnvironment<UI_MESSAGE extends UIMessage$1 = UIMessage$1> = {
  id: string;
  isMainThread: boolean;
  getThreadListItem: () => InitializableThreadListItem | undefined;
  stopOnClientDestroy?: boolean;
  /**
   * An externally owned chat instance. State lives on the instance, so it
   * survives the hosting resource unmounting; construction options are read
   * from the instance.
   */
  chat?: Chat<UI_MESSAGE> | undefined;
};
/**
 * Splits the combined options into the assistant-ui side and the `ChatInit`
 * remainder the AI SDK consumes, so external `Chat` construction forwards the
 * same fields `useChat` would.
 */
declare const splitChatThreadOptions: <UI_MESSAGE extends UIMessage$1>(options: ChatThreadOptions<UI_MESSAGE> | undefined) => {
  adapters: ({
    attachments?: import("@assistant-ui/core").AttachmentAdapter | undefined;
    speech?: import("@assistant-ui/core").SpeechSynthesisAdapter | undefined;
    dictation?: import("@assistant-ui/core").DictationAdapter | undefined;
    voice?: import("@assistant-ui/core").RealtimeVoiceAdapter | undefined;
    feedback?: import("@assistant-ui/core").FeedbackAdapter | undefined;
    threadList?: import("@assistant-ui/core").ExternalStoreThreadListAdapter | undefined;
  } & {
    history?: import("@assistant-ui/core").ThreadHistoryAdapter | undefined;
    suggestion?: import("@assistant-ui/core").SuggestionAdapter | undefined;
  }) | undefined;
  transport: ChatTransport<UI_MESSAGE> | undefined;
  toCreateMessage: CustomToCreateMessageFunction | undefined;
  onResume: ((config: import("@assistant-ui/core").ResumeRunConfig) => Promise<void>) | undefined;
  onResumeToolCall: ((options: {
    toolCallId: string;
    payload: unknown;
  }) => void) | undefined;
  onResumeError: ((error: unknown) => void) | undefined;
  joinStrategy: import("@assistant-ui/core/store").JoinStrategy | undefined;
  chatInit: {
    id?: string;
    messageMetadataSchema?: import("ai").FlexibleSchema<UI_MESSAGE extends UIMessage$1<infer METADATA, import("ai").UIDataTypes, import("ai").UITools> ? METADATA : unknown>;
    dataPartSchemas?: (UI_MESSAGE extends UIMessage$1<unknown, infer DATA_TYPES extends import("ai").UIDataTypes, import("ai").UITools> ? DATA_TYPES : import("ai").UIDataTypes) extends (infer T extends import("ai").UIDataTypes) ? { [K in keyof T]: import("ai").FlexibleSchema<T[K]>; } : never;
    messages?: UI_MESSAGE[];
    generateId?: import("ai").IdGenerator;
    onError?: import("ai").ChatOnErrorCallback;
    onToolCall?: import("ai").ChatOnToolCallCallback<UI_MESSAGE>;
    onFinish?: import("ai").ChatOnFinishCallback<UI_MESSAGE>;
    onData?: import("ai").ChatOnDataCallback<UI_MESSAGE>;
    sendAutomaticallyWhen?: (options: {
      messages: UI_MESSAGE[];
    }) => boolean | PromiseLike<boolean>;
  };
};
declare const useChatThread: <UI_MESSAGE extends UIMessage$1 = UIMessage$1>(options: ChatThreadOptions<UI_MESSAGE> | undefined, env: ChatThreadEnvironment<UI_MESSAGE>) => AssistantRuntime;
//#endregion
export { ChatThreadEnvironment, ChatThreadOptions, splitChatThreadOptions, useChatThread };
//# sourceMappingURL=useChatThread.d.ts.map