import { ChatThreadOptions } from "./useChatThread.js";
import { UIMessage } from "@ai-sdk/react";
//#region src/runtime/AISDKChat.d.ts
type AISDKChatOptions<UI_MESSAGE extends UIMessage = UIMessage> = ChatThreadOptions<UI_MESSAGE>;
/**
 * `AuiConfig` entry that runs the AI SDK chat as the `threads` scope. Hosts the
 * same orchestration as `useChatRuntime` inside the client's own resource tree,
 * so it works with any `AssistantClient` host, React or not. Single thread; the
 * multi-thread and assistant-cloud surface is {@link AISDKThreads}. The chat
 * id is captured when the entry mounts, so a later `id` change in the options
 * has no effect.
 */
declare const AISDKChat: <UI_MESSAGE extends UIMessage = UIMessage<unknown, import("ai").UIDataTypes, import("ai").UITools>>(options?: AISDKChatOptions<UI_MESSAGE> | undefined) => import("@assistant-ui/tap").ResourceElement<import("@assistant-ui/store").ClientOutput<"threads">>;
//#endregion
export { AISDKChat, AISDKChatOptions };
//# sourceMappingURL=AISDKChat.d.ts.map