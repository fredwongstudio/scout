import { ClientElement, ClientNames } from "./types/client.js";
import { DerivedElement } from "./Derived.js";
//#region src/AuiConfig.d.ts
declare const auiConfigBrand: unique symbol;
type AuiConfig = AuiConfig.Input & {
  readonly [auiConfigBrand]: true;
};
declare namespace AuiConfig {
  type Input = { [K in ClientNames]?: ClientElement<K> | DerivedElement<K>; };
}
/**
 * Builds a config for {@link AuiProvider}; the `config` prop only accepts
 * configs built with this helper.
 *
 * A config is plain data: it can be hoisted to module scope, created inline
 * per render, or memoized — the provider never relies on config identity.
 *
 * @example
 * ```tsx
 * const aui = useAui();
 * const config = AuiConfig({
 *   message: Derived({
 *     source: "thread",
 *     query: { index: 0 },
 *     get: (aui) => aui.thread.message({ index: 0 }),
 *   }),
 * });
 *
 * <AuiProvider extends={aui} config={config}>{children}</AuiProvider>;
 * ```
 */
declare const AuiConfig: (config: AuiConfig.Input) => AuiConfig;
//#endregion
export { AuiConfig };
//# sourceMappingURL=AuiConfig.d.ts.map