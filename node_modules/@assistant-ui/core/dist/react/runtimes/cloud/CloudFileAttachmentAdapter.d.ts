import { Attachment, CompleteAttachment, PendingAttachment } from "../../../types/attachment.js";
import { AttachmentAdapter } from "../../../adapters/attachment.js";
import { AssistantCloud } from "assistant-cloud";
//#region src/react/runtimes/cloud/CloudFileAttachmentAdapter.d.ts
declare class CloudFileAttachmentAdapter implements AttachmentAdapter {
  accept: string;
  private getCloud;
  constructor(cloud: AssistantCloud);
  constructor(getCloud: () => AssistantCloud);
  private uploadedUrls;
  add({ file }: {
    file: File;
  }): AsyncGenerator<PendingAttachment, void>;
  remove(attachment: Attachment): Promise<void>;
  send(attachment: PendingAttachment): Promise<CompleteAttachment>;
}
//#endregion
export { CloudFileAttachmentAdapter };
//# sourceMappingURL=CloudFileAttachmentAdapter.d.ts.map